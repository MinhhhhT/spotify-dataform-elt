-- config {
--   type: "assertion",
--   name: "assert_track_audit_quarantine_logic",
--   tags: ["staging", "assertion"],
-- }

-- -- check that all records that are not valid are moved to the quarantine table
-- SELECT
--   *
-- FROM
--   ${ref("stg_track_validation")}
-- WHERE
--   validation_status != "valid"
--   AND track_uri NOT IN (
--     SELECT
--       track_uri
--     FROM
--       ${ref("stg_track_quarantine")}
--   )